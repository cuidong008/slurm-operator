
## slurm-web-docker

是生成的用了部署slurm-web项目的文档和dockerfile等

## scow-slurm-adapter

OpenSCOW 用的 Slurm adapter：**默认**用 `scow-slurm-adapter/docker-compose.yml` 在本机 Docker 跑；可选 K8s 清单为 `scow-slurm-adapter.k8s.yaml`。说明见 `scow-slurm-adapter/README.md`。

